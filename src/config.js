const config = {};
config['USERNAME'] = 'Ruonyy';
config['SIGN_MESSAGE'] = `Sign by ${config.USERNAME}`;
config['LOG_LEVEL'] = 'debug';
config['LOG_EMOJI'] = 'true';
config['WHITELIST'] = ['CanisMinor'];

module.exports = config;
